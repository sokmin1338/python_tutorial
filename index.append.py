# 시퀸스 객체의 각 요소는 순서가 정해져 있으며, 이 순서를 '인덱스'라고 부름
# 시퀸스 객체에 []를 붙이고 []안에 각요소의 인덱스를 지정하면 해당 요소에 접근 할 수 있음

# A[1, 2, 3, 4, 5]
# A[0] = 1
# A[1] = 2


# range도 인덱스로 접근 가능
# r = range(0, 10, 2)
# r[2] = 4


# 문자열은 요소가 문자이므로 인덱스로 접근하면 문자가 나옴(range도 문자열 가능)

# A = "Sokmin"
# A[0] = S
# A[0:] = Sokmin

# 음수 인덱스 지정하기(range도 가능)

# A = [10, 21, 03, 15]
# A[-1] = 15


